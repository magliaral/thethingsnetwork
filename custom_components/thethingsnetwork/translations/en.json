{
  "entity": {
    "button": {
      "downlink_on": {
        "name": "{name} On"
      },
      "downlink_off": {
        "name": "{name} Off"
      }
    }
  },
  "config": {
    "abort": {
      "already_configured": "Application ID is already configured",
      "reauth_successful": "Re-authentication was successful",
      "reconfigure_successful": "Reconfiguration was successful"
    },
    "error": {
      "invalid_auth": "Invalid authentication",
      "unknown": "Unexpected error"
    },
    "step": {
      "reauth_confirm": {
        "description": "The Things Network application could not be connected.\n\nPlease check your credentials."
      },
      "reconfigure": {
        "data": {
          "first_fetch_h": "Initial fetch period"
        },
        "data_description": {
          "first_fetch_h": "How many hours of historical data to fetch"
        }
      },
      "user": {
        "data": {
          "api_key": "API key",
          "app_id": "Application ID",
          "host": "Host"
        },
        "description": "Enter the API hostname, application ID and API key to use with Home Assistant.\n\n[Read the instructions]({instructions_url}) on how to register your application and create an API key.",
        "title": "Connect to The Things Network v3"
      }
    }
  },
  "selector": {
    "first_fetch_h": {
      "options": {
        "1": "Last 1 hour",
        "12": "Last 12 hours",
        "24": "Last 24 hours",
        "3": "Last 3 hours",
        "6": "Last 6 hours"
      }
    }
  },
  "services": {
    "send_downlink": {
      "name": "Send downlink",
      "description": "Schedule a confirmed raw downlink for a TTN device (replaces pending commands on the same fPort, keeps other fPorts queued).",
      "fields": {
        "device_id": {
          "name": "Device ID",
          "description": "TTN end device ID (e.g. eui-...)."
        },
        "fport": {
          "name": "fPort",
          "description": "LoRaWAN fPort of the channel (1-223)."
        },
        "text": {
          "name": "Text",
          "description": "ASCII payload, max 51 bytes (EU868 DR0). Mutually exclusive with payload_base64."
        },
        "payload_base64": {
          "name": "Payload (base64)",
          "description": "Raw payload as base64. Mutually exclusive with text."
        },
        "confirmed": {
          "name": "Confirmed",
          "description": "Retransmit until the device acknowledges (default on)."
        }
      }
    }
  }
}
